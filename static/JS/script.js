const s = document.querySelector(".navbar-form input"); const i = document.querySelectorAll(".movie-card"); document.querySelector("#filterForm").addEventListener("submit", (e) => { e.preventDefault(); setTimeout(filterMovies, 1000); }); s.addEventListener("keyup", (e) => { if (e.key === "Enter") { e.preventDefault(); i.forEach((t) => { t.style.display = t.dataset.name.toLowerCase() === s.value.toLowerCase() ? "block" : "none"; }); } }); s.addEventListener("keyup", () => { if (s.value === "") { i.forEach((t) => (t.style.display = "block")); } }); document.getElementById("filterInput").autocomplete = "off"; document.querySelector(".navbar-form-btn").addEventListener("click", () => { const enterEvent = new KeyboardEvent("keyup", { key: "Enter" }); s.dispatchEvent(enterEvent); });