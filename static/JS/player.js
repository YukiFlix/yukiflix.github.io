var player = document.getElementById('video-player'); function playVideo(videoSrc) { player.src = videoSrc; var iframes = document.getElementsByTagName('iframe'); for (var i = 0; i < iframes.length; i++) { if (iframes[i] !== player) { iframes[i].contentWindow.postMessage('{"event":"command","func":"pauseVideo","args":""}', '*'); } } } var defaultVideoSrc = document.getElementById('active').getAttribute('video-src'); playVideo(defaultVideoSrc);